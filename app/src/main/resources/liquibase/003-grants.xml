<?xml version="1.0" encoding="UTF-8"?>
<databaseChangeLog
        xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
        xmlns="http://www.liquibase.org/xml/ns/dbchangelog"
        xsi:schemaLocation="http://www.liquibase.org/xml/ns/dbchangelog http://www.liquibase.org/xml/ns/dbchangelog/dbchangelog-3.1.xsd">

    <changeSet id="SMI-16373-app-user-grants-on-all" author="d.v.kornilovich" runAlways="true">
        <sql endDelimiter=";">
            grant usage on schema "${schema}" to "${appUsername}";
            grant select, insert, update, delete, references on all tables in schema "${schema}" to "${appUsername}";
            grant select, usage on all sequences in schema "${schema}" to "${appUsername}";
        </sql>
    </changeSet>
</databaseChangeLog>
