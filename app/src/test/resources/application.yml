invest:
  enabled: false
  connector:
    token: token

ru:
  invest:
    api:
      stock:
        supplier:
          scheduler:
            enabled: false
            cron: "0 0 0 * * ?"
spring:
  cache:
    type: caffeine
  cloud:
    openfeign:
      autoconfiguration:
        jackson:
          enabled: true
      client:
        config:
          cb-rf-client:
            enabled: false
            url: localhost:8082
          budget-org-client:
            enabled: false
            url: localhost:8082